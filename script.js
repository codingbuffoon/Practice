// Variables
let adjective = "hairy ";
let noun = "Bob ";
let verb = "running";
let sentence = adjective + noun + verb;

// Loop
let count = 0;
for (let x = 0; x < Infinity; x++) {
  count += 2;
}
console.log(count);